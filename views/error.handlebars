<main>
  <div class="error-container">
    <h2>Error</h2>
    {{#if errorMessage}}
      <div class="error-message">
        {{errorMessage}}
      </div>
    {{else}}
      <div class="error-message">
        An unknown error occurred.
      </div>
    {{/if}}

    <div class="error-actions">
      {{#each navLink}}
        <a href="{{this.link}}" class="button">{{this.text}}</a>
      {{else}}
        <a href="/" class="button">Return to Home</a>
      {{/each}}
    </div>
  </div>
</main>